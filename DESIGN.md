# Grow Flow — дизайн-система

Маркетинговое агентство полного цикла. Один лендинг, кинематографичный
и «студийный» по тону: тёмная HUD-эстетика съёмочной площадки (REC-индикатор,
таймкоды, статус-бары) в hero и «Знакомстве», дальше — чистый редакционный
светлый/тёмный поток без карточек-коробок. Референс по духу (не по копированию
текста/брендинга) — kookie-kollective.com: разрыв палитры между разделами,
крупная типографика, минимум декоративных рамок.

## Тон и принципы

- **Не агентство-конструктор, а студия.** HUD-детали (REC, таймкоды,
  счётчики) читаются как техническая метка происходящей съёмки/работы,
  а не как украшение — используются точечно, не на каждом экране.
- **Одно непрерывное полотно, а не стопка карточек.** Фон страницы не
  переключается дискретно между секциями — цвет непрерывно
  интерполируется по позиции скролла (см. «Тема и фон» ниже). Разделы
  не обведены рамками/тенями, граница — это смена фона и типографики.
- **Крупная типографика как основной визуальный вес**, а не иллюстрации.
  Один засечный дисплейный шрифт для смысловых заголовков, гротеск —
  для всего остального (лейблы, тело, UI).
- **Сдержанная анимация.** Motion усиливает восприятие (параллакс,
  плавное появление, кроссфейд темы), но не развлекает само по себе;
  `prefers-reduced-motion` уважается везде.

## Цвет

Два полюса, между которыми страница непрерывно едет (не жёсткие
блоки-темы — RGB-интерполяция по скроллу):

| Токен | Тёмная тема | Светлая тема | Назначение |
|---|---|---|---|
| `--bg` | `#0a0a0a` | `#f2efe7` (242,239,231) | фон полотна |
| `--fg` | `#f5f2ea` | `#0c0b09` (12,11,9) | основной текст |
| `--fg-dim` | `#8f8b82` (143,139,130) | `#6d6858` (109,104,92) | вторичный текст/лейблы |
| `--line` | `rgba(245,242,234,.12)` | `rgba(12,11,9,.16)` | тонкие разделители |
| `--accent` (адаптивный) | `#c9a668` (тёплое золото) | почти-чёрный `#0c0b09` | ссылки/hover прямо на фоне — сам держит контраст на любой теме |

Фиксированный акцент бренда (не участвует в интерполяции, всегда
одинаковый — только для залитых кнопок, текст поверх тёмный,
контраст 8.56:1):

- `--accent-brand: #c9a668`
- `--accent-brand-ink: #0c0b09`

Локальный синий акцент только в секции «Знакомство» (`#sceneA`) —
`--accent: #5aa3e8`, подхватывает цвет света в самом видео/кадре, не
используется больше нигде.

Нейтральные — не чистый чёрный/белый: тёплый почти-чёрный `#0a0a0a` и
тёплый неполный-белый `#f2efe7`/`#f5f2ea` (лёгкий крем, не холодный
серый) — оба сдвинуты к той же тёплой температуре, что и золотой акцент.

## Типографика

Три роли, три разных гарнитуры — не смешивать назначение:

- **Дисплейная (смысловые заголовки h2, крупные цитаты-хедлайны):**
  `'Playfair Display', Georgia, serif` — засечная, курсив используется
  под акцентные короткие фразы.
- **Текстовая (UI, body, h1 лого):** `'Inter', -apple-system, sans-serif`.
- **Утилитарная (лейблы, навигация, теги, таймкоды, нумерация разделов):**
  `'JetBrains Mono', monospace` — всегда с letter-spacing и
  `text-transform:uppercase` для лейблов/eyebrow-строк.

Шкала (clamp — не фиксированные px, тянется по вьюпорту):

```
--fs-h1: clamp(40px, 7.4vw, 86px)
--fs-h2: clamp(48px, 7.2vw, 110px)
--fs-h3: clamp(22px, 2.6vw, 32px)
--fs-body: 16px
--fs-small: 14px
--lh-heading: 0.98   (плотные заголовки)
--lh-body: 1.65      (свободное тело)
```

Мелкие uppercase-лейблы (eyebrow, теги, HUD-строка) — `--fs-small`
или мельче, `letter-spacing` заметный (0.1–0.16em), моно-гарнитура.

## Отступы и сетка

```
--space-xs: 8px
--space-sm: 16px
--space-md: 28px
--space-lg: 56px
--space-xl: 96px
--container-pad: clamp(20px, 6vw, 96px)   /* горизонтальный паддинг секций */
```

Один брейкпоинт: **`max-width: 820px`** — переход в мобильную раскладку
(одна колонка, вкладочные рейлы/параллакс отключаются, карточки кейсов
из абсолютного позиционирования уходят в простой вертикальный список).

## Форма и компоненты

- **Скругления по смыслу, не единая шкала:** `100px` (полная пилюля) —
  только интерактивные CTA-кнопки и теги/пилюли; `2–3px` (почти острый
  угол) — карточки, медиа-контейнеры, текстовые плашки поверх видео;
  `8px` — поля форм; `14–26px` — телефон-мокап (единственное место с
  выраженным скруглением, потому что имитирует экран устройства).
- **Кнопки:** заливка `--accent-brand`, текст `--accent-brand-ink`,
  либо контурная (`border:1px solid var(--accent-brand)`, прозрачный
  фон) — обе всегда pill-shape (`border-radius:100px`).
- **Тонкие обводки** (`1px solid var(--line)`) вместо теней/карточек-боксов
  — основной способ обозначить границу компонента (тег, поле формы).
- **HUD-индикаторы** (REC-точка с пульсацией, статус-строка, счётчики) —
  моно-шрифт, uppercase, мелкий кегль, используются в hero и как
  прогресс-рейл сбоку страницы.

## Motion

- Единый easing по всему сайту: `cubic-bezier(.16,.84,.44,1)`.
- **Reveal-по-скроллу:** блоки появляются через `opacity + translateY`
  (~40px) при входе в вьюпорт, один раз (`IntersectionObserver`), не
  time-based рывок, а плавный transition (~0.6–1s).
- **Фон/тема:** цвет фона и текста — не дискретный переключатель, а
  прямая функция позиции скролла (широкая переходная зона ~0.9 высоты
  вьюпорта по обе стороны границы), поэтому скролл в любую сторону/
  с любой скоростью не даёт видимого «шва».
- **Параллакс** — точечно (карточки кейсов, предметные картинки услуг),
  всегда отключается на `pointer:coarse` (тач) и `prefers-reduced-motion`.
- Никакого скролл-хайджекинга (без Lenis/Locomotive/GSAP ScrollTrigger) —
  скролл нативный, кастомный слой только лёгкое lerp-сглаживание
  производных визуальных эффектов.

## Медиа

- Видео — фоновое, всегда `autoplay muted loop playsinline`,
  `object-fit:cover`, с `poster` на первый кадр и градиентным
  затемнением поверх для читаемости текста.
- Съёмка — тёплый/синеватый ночной свет комнаты, вид сверху на
  рабочий стол (top-down desk shot) — сквозной визуальный мотив по
  всему сайту (hero + «Знакомство»).

## Структура страницы

1. **Hero** — полноэкранное видео, лого + заголовок «Grow Flow°» +
   подзаголовок + CTA-кнопка, HUD-строка сверху.
2. **Услуги** — 4 карточки друг за другом, светлая тема, вкладочный
   рейл сбоку, крупное слово-заголовок услуги.
3. **Знакомство** — pinned-сцена, scroll-scrubbing кадров, синий
   локальный акцент, текстовая плашка проявляется ближе к концу.
4. **Кейсы** — тёмная тема, доска карточек с параллаксом и наклоном
   (та же тёмная опора темы, что и «Знакомство» — перехода фона между
   ними нет).
5. **Клиенты** — бегущая строка логотипов.
6. **Контакты** — форма + телефон-мокап, светлая тема.
7. **Финальный CTA + футер** — полноэкранная светлая сцена, компактный
   информационный футер под ней.
